server:
    port: 8080
    
spring:
    application:
        name: course
    jpa:        
        hibernate:
            ddl-auto: update
        show-sql: false
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL5Dialect
    jmx:
        enabled: false
    datasource:
        url: jdbc:mysql://localhost:3306/course?allowPublicKeyRetrieval=true&useSSL=false
        username: root
        password: microsservicos
        driver-class-name: com.mysql.cj.jdbc.Driver       